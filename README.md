# LLM-Based-Search-of-Product-Listings
LLM-based hybrid search system for online second-hand marketplace listings (Final Year Project).

Files:
main.py (main search system),
embeddings.py (generates embeddings),
500_listings_realistic.json (stores dataset of listings),
listing_vectors.json (stores listing vectors).
